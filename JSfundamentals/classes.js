

// when having an object with more than one method create a "blue print to create objects of that type";

class Person {
    constructor(name) {
        this.name = name
    }
    walk() {
    console.log(walk);
}
}
