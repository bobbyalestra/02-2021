

// when having an object with more than one method create a "blue print";


const person = {
    name : 'Bobby',
    walk() {
        console.log(walk);
    }
};

const person2 = {
    name : 'Bobby',
    walk() {
        console.log(walk);
    }
};